export interface SlideProps {
	img: {
		url: string;
		width: number;
		height: number;
	};
	title: string;
	description: string;
	id: number;
}

import MainSlide from './MainStyle';
export default MainSlide;
